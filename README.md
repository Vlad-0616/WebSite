# Перевозки.Бел — Биржа грузоперевозок

## Описание проекта

**Перевозки.Бел** — это динамическая веб-платформа для прямого взаимодействия грузовладельцев и перевозчиков. Заказчики могут быстро разместить заявку на перевозку груза, а водители или транспортные компании — находить подходящие заказы и предлагать свои услуги. Платформа автоматизирует процесс поиска транспорта и отслеживания статуса перевозки, делая логистику прозрачной и эффективной.

## Целевая аудитория

| Сегмент | Описание |
|---------|----------|
| **Грузовладельцы** | Компании и частные лица, которым нужно перевезти грузы |
| **Перевозчики** | Владельцы грузового транспорта (ИП, компании, частные водители) |
| **Логисты и экспедиторы** | Специалисты, занимающиеся поиском транспорта под задачи клиентов |

---

## Ключевой функционал

- **Управление заказами:** размещение заказов на перевозку с указанием веса, габаритов, маршрута, типа погрузки, вида прицепа и стоимости
- **Управление транспортом:** добавление и редактирование транспортных средств перевозчиками
- **Поиск и фильтрация:** по направлениям, весу, типу кузова, дате загрузки, цене, типу загрузки
- **Личные кабинеты:** раздельные интерфейсы для заказчиков и перевозчиков

---

## Структура приложения

| Раздел | Маршрут | Доступ | Описание |
|--------|---------|--------|----------|
| Главная | `/` | Публичный | Страница с быстрым поиском, описанием и предложением зарегистрироваться или войти (если не в учетной записи) |
| Аутентификация | `/auth/*` | Публичный | Вход, регистрация, восстановление пароля через кодовое слово |
| Каталог заказов | `/orders` | Публичный | Список заказов с фильтрацией и пагинацией |
| Детальная страница заказа | `/orders/:id` | Публичный | Подробности заказа, возможность принять заказ (для перевозчика) |
| Каталог транспорта | `/trucks` | Публичный | Список доступного транспорта с фильтрацией |
| Личный кабинет (Заказчик) | `/profile` | Приватный | Мои заказы, создание нового заказа |
| Личный кабинет (Перевозчик) | `/profile/transporter` | Приватный | Мои заказы (в работе), управление транспортом |

---

## Технологический стек

### Frontend

| Технология | Назначение | Обоснование |
|------------|------------|-------------|
| **Vue 3 (Composition API)** | UI фреймворк | Низкий порог входа, реактивность из коробки, быстрая разработка, отличная производительность |
| **JavaScript** | Типизация | Безопасная работа со сложными объектами (груз, маршрут), предотвращение ошибок |
| **Vue Router 4** | Маршрутизация | Официальное решение, защищённые маршруты, вложенные роуты для личных кабинетов |
| **Pinia** | State-менеджмент | Официальное решение Vue, простое API, отличная интеграция с DevTools, поддержка TypeScript |
| **VeeValidate + Zod** | Формы и валидация | Простая работа со сложными динамическими формами (создание заказа, управление транспортом). Единые схемы валидации между клиентом и сервером |
| **Axios** | HTTP клиент | Удобная настройка базового URL, перехватчики (interceptors) для автоматической подстановки JWT токенов и централизованной обработки ошибок |
| **Vite** | Сборка | Быстрая HMR, оптимизированная production-сборка, мгновенный старт разработки |
| **PrimeVUE + Tailwind CSS** | UI компоненты и стилизация | Готовые компоненты (таблицы, модалки, инпуты) + гибкая кастомизация через Tailwind, адаптивный дизайн |

### Backend

| Технология | Назначение | Обоснование |
|------------|------------|-------------|
| **Node.js (Express)** | Backend фреймворк | Модульная архитектура, встроенная поддержка WebSockets, единый язык с фронтендом |
| **JavaScript** | Типизация | Общие DTO с фронтендом, строгая типизация, единые схемы Zod |
| **Supabase (PostgreSQL)** | База данных | ACID-транзакции для безопасного бронирования заказов, поддержка PostGIS для геоданных |
| **JWT (Access)** | Аутентификация | Безопасное разграничение ролей (shipper / carrier) |
| **bcrypt** | Хеширование | Надёжное хеширование паролей и кодовых слов |

### Инфраструктура

| Компонент | Выбор | Обоснование |
|-----------|-------|-------------|
| Frontend хостинг | Vercel / Netlify | Автоматический деплой из GitHub, CDN, поддержка Vue SPA |
| Backend хостинг | Railway.app / Render.com | Простой деплой Node.js, встроенная БД |
| База данных | Supabase (PostgreSQL) | Готовый PostGIS, автобэкапы, удобный клиент, встроенная аутентификация (опционально) |
| CI/CD | GitHub Actions | Автоматическое тестирование и деплой при пуше |

### Методологии

| Методология | Описание |
|-------------|----------|
| **Feature-based структура** | Организация кода по функциональным модулям (`orders/`, `auth/`, `profile/`, `trucks/`) |
| **API-стиль** | REST для всех операций CRUD |
| **Безопасность** | Helmet.js, CORS, валидация Zod |

---

## Визуальное проектирование интерфейса

### Общие принципы UI/UX

Для обеспечения единообразия пользовательского опыта на всех страницах приложения используются унифицированные подходы к отображению состояний:

| Состояние | Компонент | Поведение |
|-----------|-----------|----------|
| **Загрузка (Loading)** | Скелетоны (Skeleton) | Отображаются заглушки, повторяющие структуру контента (карточки, строки таблицы). Используются для первичной загрузки страниц и пагинации |
| **Ошибка (Error)** | Toast + Fallback | Системные ошибки (сеть, 500) отображаются в виде всплывающего уведомления (toast). Критические ошибки на странице сопровождаются кнопкой "Повторить запрос" |
| **Пустое состояние (Empty)** | Иллюстрированная заглушка | Если данных нет (нет заказов, нет транспорта), показывается понятное сообщение и кнопка действия (например, "Создать первый заказ") |
| **Debounce** | Задержка запроса | При вводе текста в поиск или фильтры (геокодер, поиск по заказам) запрос на сервер отправляется с задержкой 300-500мс для снижения нагрузки |

---

### Страница: Главная (`/`)

**Тип доступа:** публичный

**Основные блоки:**

| № | Блок | Описание | Источник данных |
|---|------|----------|-----------------|
| 1 | Шапка | Логотип, меню, кнопка "Войти"/профиль | GET /api/user/me |
| 2 | Герой-секция | Заголовок, кнопка действия в зависимости от роли | Статичный |
| 3 | Быстрый поиск | Поля "Откуда", "Куда", кнопка "Найти" | GET /api/geocode/suggest |
| 4 | Последние заказы | Карусель активных заказов (6 шт.) | GET /api/orders?sort=latest&limit=6 |
| 5 | Популярные направления | Топ-5 маршрутов | GET /api/statistics/popular-routes |
| 6 | Футер | Контакты, ссылки, соцсети | Статичный |

**Динамические состояния:**

🔹 **Последние заказы:**
- **Загрузка:** скелетоны карточек (6 шт.)
- **Пусто:** "Нет активных заказов" + кнопка "Разместить заказ"
- **Ошибка:** сообщение + кнопка "Повторить"

---

### Страница: Аутентификация (`/auth/*`)

**Тип доступа:** публичный

#### Регистрация (`/auth/register`)

| Шаг | Блок | Описание | API |
|-----|------|----------|-----|
| 1 | Выбор роли | Переключатель "Заказчик" / "Перевозчик" | — |
| 2 | Контакты | Email, телефон, пароль | POST /api/auth/check-email |
| 3 | Данные пользователя | Имя | — |
| 4 | Для восстановления пароля | Кодовое слово, подсказка к слову | — |

**Состояния:**
- Валидация в реальном времени Zod
- Отправка: спиннер на кнопке
- Успех: редирект на `/`

#### Вход (`/auth/login`)

| Блок | Описание | API |
|------|----------|-----|
| Форма входа | Email/телефон, пароль, "Запомнить меня" | POST /api/auth/login |
| Доп. ссылки | "Забыли пароль?", "Создать аккаунт" | — |

**Состояния:**
- Неверные данные: сообщение об ошибке
- Успех: редирект на предыдущую страницу

#### Восстановление пароля (`/auth/recover`)

| Шаг | Блок | API |
|-----|------|-----|
| 1 | Ввод email, получение подсказки | POST /api/auth/recover/request |
| 2 | Ввод кодового слова | POST /api/auth/recover/verify |
| 3 | Новый пароль | POST /api/auth/recover/reset |

---

### Страница: Каталог заказов (`/orders`)

**Тип доступа:** публичный

**Основные блоки:**

| № | Блок | Описание | API |
|---|------|----------|-----|
| 1 | Поисковая строка | "Откуда", "Куда", кнопка "Найти" | GET /api/geocode/suggest |
| 2 | Панель фильтров | Вес, тип кузова, дата, цена (сворачиваемый) | GET /api/filters/options |
| 3 | Список заказов | Сетка карточек с пагинацией | GET /api/orders?page=... |

**Динамика:**
- Изменение фильтра → автоматический перезапрос 
- Фильтры сохраняются в URL 
- **Состояния:** скелетоны → "нет заказов" → ошибка

---

### Страница: Детальная страница заказа (`/orders/:id`)

**Тип доступа:** публичный

**Основные блоки:**

| № | Блок | Описание | API |
|---|------|----------|-----|
| 1 | Заголовок | Маршрут, статус, дата, ID | GET /api/orders/:id |
| 2 | Детали груза | Вес, объем, габариты, тип погрузки | Поля заказа |
| 3 | Требования к транспорту | Тип кузова | Поля заказа |
| 4 | Финансы | Цена, валюта | Поля заказа |
| 5 | Заказчик | Название | GET /api/users/:id |

**Статусы заказа:**
- `active` — открыт для предложений
- `in_progress` — в работе
- `completed` — выполнен
- `canceled` — отменён

---

## Личный кабинет перевозчика (`/profile/transporter`)

**Тип доступа:** приватный (только для авторизованных пользователей с ролью `carrier`)

### Структура маршрутов

| Раздел | Маршрут | Доступ | Описание |
|--------|---------|--------|----------|
| Мои заказы | `/profile/transporter/orders` | Приватный | Список принятых заказов в работе. Возможность отказаться |
| Управление транспортом | `/profile/transporter/trucks` | Приватный | CRUD операции с транспортными средствами: добавление, редактирование, удаление. Управление статусом доступности |
| Настройки профиля | `/profile/transporter/settings` | Приватный | Управление личными данными |

**Управление транспортом:**
- Добавление: тип кузова, грузоподъемность, объем, госномер, марка, модель, год, локация
- Редактирование, удаление, смена статуса доступности

---

## Схема данных и API

## Ключевые эндпоинты API

### Аутентификация

| Метод | Эндпоинт | Описание | Тело запроса | Ответ |
|-------|----------|----------|--------------|-------|
| `POST` | `/api/auth/register` | Регистрация нового пользователя | `{ email, password, name, phone?, role, secretWord, hintQuestion }` | `{ user, access_token }` |
| `POST` | `/api/auth/login` | Вход в аккаунт | `{ email, password, remember_me? }` | `{ user, access_token }` |
| `POST` | `/api/auth/logout` | Выход из аккаунта | `{ refresh_token }` | `{ user }` |
| `GET` | `/api/auth/me` | Получение данных текущего пользователя | `{ refresh_token }` | `{ user }` |
| `POST` | `/api/auth/recover/request` | Запрос подсказки на восстановление пароля | `{ email }` | `{ hint_question }` |
| `POST` | `/api/auth/recover/verify` | Проверка кодового слова | `{ email, secretWord }` | `{ message }` |
| `POST` | `/api/auth/recover/reset` | Сброс пароля | `{ email, secretWord, password, confirmPassword }` | `{ message }` |

---

### Заказы

| Метод | Эндпоинт | Описание | Доступ | Параметры / Тело |
|-------|----------|----------|--------|------------------|
| `GET` | `/api/orders` | Список заказов с фильтрацией и пагинацией | Публичный | `?page=1&limit=20&status=active&from=Minsk&to=Brest&weight_min=1000&truck_type=refrigerator` |
| `GET` | `/api/orders/:id` | Детальная информация о заказе | Публичный | `:id` - UUID заказа` |
| `GET` | `/api/orders/my` | Заказы текущего заказчика | shipper | `-` |
| `GET` | `/api/orders/my-active` | Активные заказы (в работе) для заказчика/перевозчика | shipper, carrier | `-` |
| `POST` | `/api/orders` | Создание нового заказа | `shipper` | `{ pickup_address, delivery_address, weight_kg, price, volume_m3?, truck_type?, loading_type?, loading_date?, description? }` |
| `PATCH` | `/api/orders/:id` | Редактирование заказа | `shipper` (только свой, статус active) | `{ ...обновляемые поля }` |
| `DELETE` | `/api/orders/:id` | Отмена заказа | `shipper` (только свой) | — |
| `POST` | `/api/orders/:orderId/accept` | Принять заказ в работу | `carrier` | `-` |
| `POST` | `/api/orders/:orderId/cancel-assignment` | Отменить назначение перевозчика | `shipper, carrier` (по своему заказу) | — |

### Транспорт

| Метод | Эндпоинт | Описание | Доступ | Тело запроса / Параметры |
|-------|----------|----------|--------|--------------------------|
| `GET` | `/api/trucks` | Список доступного транспорта с фильтрацией | Публичный | `?truck_type=&capacity_min=&capacity_max=&location=&volume_min=&volume_max=` |
| `GET` | `/api/trucks/my` | Мой транспорт | `carrier` | — |
| `GET` | `/api/trucks/:id` | Детали транспорта | Публичный | — |
| `POST` | `/api/trucks` | Добавить транспортное средство | `carrier` | `{ truck_type_id, capacity_kg, plate_number, brand?, model?, year?, capacity_m3?, dimensions?, location?, description? }` |
| `PATCH` | `/api/trucks/:id` | Редактировать транспорт | `carrier` (свой) | `{ ...обновляемые поля }` |
| `PATCH` | `/api/trucks/:id/availability` | Изменить статус доступности | `carrier` (свой) | `{ available }` |
| `DELETE` | `/api/trucks/:id` | Удалить транспорт | `carrier` (свой) | — |
| `GET` | `/api/truck-types` | Справочник типов транспорта | Публичный | — |

---

### Пользователи и профиль

| Метод | Эндпоинт | Описание | Доступ | Тело запроса |
|-------|----------|----------|--------|--------------|
| `GET` | `/api/auth/me` | Мой профиль | Авторизованный | — |
| `GET` | `/api/users/:id` | Публичный профиль (не реализован) | Публичный | — |
| `PATCH` | `/api/auth/profile` | Редактирование профиля (заглушка в коде) | Авторизованный | `{ name, phone }` |

---

### Поиск и справочники

| Метод | Эндпоинт | Описание | Параметры |
|-------|----------|----------|-----------|
| `GET` | `/api/statistics/popular-routes` | Популярные маршруты | `?limit=5` |
| `GET` | `/api/truck-types` | Типы транспортных средств | — |

---

### Вспомогательные эндпоинты

| Метод | Эндпоинт | Описание | Доступ |
|-------|----------|----------|--------|
| `GET` | `/api/health` | Проверка работоспособности сервера | Публичный |
